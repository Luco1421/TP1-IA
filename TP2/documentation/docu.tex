\documentclass[12pt]{article}

\usepackage[spanish,es-tabla]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{float}
\usepackage{subcaption}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{algorithm}
\usepackage{algpseudocode}

\geometry{letterpaper, margin=2.5cm}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=black,
    urlcolor=blue
}

\newcommand{\pendiente}[1]{\textcolor{red!70!black}{\textbf{Pendiente:} #1}}
\newcommand{\claseSimple}{\texttt{0}}
\newcommand{\claseCompleja}{\texttt{1}}

\begin{document}

\begin{titlepage}
\centering
{\includegraphics[width=0.7\textwidth]{logoTEC.png}\par}
\vspace{1cm}
{\bfseries\Large Segundo Trabajo Práctico\par}
\vspace{1cm}
{\scshape\Large Curso:\\
Inteligencia Artificial \par}
\vspace{3cm}
{\scshape\Huge Clasificación de textos según su complejidad \par}
\vspace{2cm}
{\Large Profesor: \\
Ph. D. Saúl Calderón Ramírez \\ \par}
\vfill
{\Large Estudiantes: \par}
{\Large Alejandro Cerdas\\
Kener Castillo\\
Pablo Pérez\par}
\vfill
{\Large \today \par}
\end{titlepage}

\tableofcontents
\newpage

\section{Introducción}

La simplificación de textos estudia métodos para transformar o seleccionar expresiones más comprensibles sin destruir su contenido semántico. En contextos de educación financiera, esta tarea resulta especialmente importante: los textos técnicos suelen contener términos especializados, estructuras sintácticas extensas y referencias conceptuales que pueden dificultar el acceso a la información. Esta dificultad es aún más relevante para poblaciones con necesidades específicas de accesibilidad, como las personas con discapacidad visual, para quienes la claridad textual puede afectar directamente la autonomía en la lectura y comprensión de materiales educativos \cite{FEINA}.

En este trabajo se aborda una tarea relacionada: clasificar segmentos de texto en español según su complejidad. Para ello se comparan cuatro familias de clasificadores: regresión logística con descriptores TF-IDF, regresión logística con vectores empotrados, y dos modelos de lenguaje grandes disponibles en Hugging Face. La clase \claseSimple{} representa textos simples, mientras que la clase \claseCompleja{} representa textos complejos. El conjunto de datos usado es FEINA, compuesto por pares de segmentos financieros simples y complejos \cite{FEINA}.

El reporte se divide en dos bloques. Primero, se implementa y analiza la regresión logística a partir de una implementación base del perceptrón, usando datos sintéticos linealmente separables y no linealmente separables. Segundo, se aplica esa regresión logística a la clasificación de textos mediante representaciones TF-IDF y vectores empotrados, y se comparan sus resultados contra clasificadores basados en LLMs bajo configuraciones sin ejemplos y con few-shot learning.

\section{Metodología general}

El objetivo del presente documento es realizar una comparación experimental entre modelos de clasificación. Por esta razón, el diseño del trabajo separa tres elementos: representación de los datos, clasificador y protocolo de evaluación. Esta separación permite interpretar si una mejora proviene del algoritmo de clasificación, de la forma en que se representa el texto o de una diferencia en las condiciones experimentales.

Para los experimentos con datos sintéticos se generan dos tipos de conjuntos: uno linealmente separable y otro no linealmente separable. En ambos casos se entrena una regresión logística y se evalúa el error medio absoluto (MAE), definido como

\begin{equation}
e_{\text{MAE}} = \frac{1}{N}\sum_{i=1}^{N}|t_i - \tilde{t}_i|,
\end{equation}

donde $t_i$ es la etiqueta esperada y $\tilde{t}_i$ es la salida estimada por el modelo.

Para los experimentos con textos, se utiliza FEINA como conjunto base. Cada par contiene un segmento complejo y una propuesta simplificada; por tanto, el problema se formula como clasificación binaria. En los modelos basados en regresión logística, el texto se transforma primero a una representación numérica. En los modelos basados en LLMs, el texto se clasifica directamente mediante instrucciones en lenguaje natural.

\section{Perceptrón}

El perceptrón fue propuesto por Rosenblatt en 1958 como un modelo probabilístico inspirado en procesos de almacenamiento y organización de información en el cerebro \cite{rosenblatt1958perceptron}. En aprendizaje supervisado, puede interpretarse como un clasificador lineal binario: aprende un vector de pesos $\mathbf{w}$ que define una frontera de decisión en el espacio de entrada. Dado un vector $\mathbf{x}$, el modelo calcula el producto interno $\mathbf{x}^{T}\mathbf{w}$ y asigna una clase según el signo del resultado:

\begin{equation}
\hat{y} = \operatorname{sign}(\mathbf{x}^{T}\mathbf{w}).
\end{equation}

La función signo convierte la salida continua del producto interno en una decisión discreta. En este contexto, si $\mathbf{x}^{T}\mathbf{w} > 0$, la muestra se asigna a la clase positiva; si $\mathbf{x}^{T}\mathbf{w} < 0$, se asigna a la clase negativa. El caso exactamente igual a cero representa una muestra ubicada sobre la frontera de decisión y suele resolverse por convención de implementación.

Esta formulación es simple y eficiente, pero impone una restricción fuerte: el modelo solo puede separar perfectamente problemas linealmente separables. Si las clases se encuentran mezcladas de forma no lineal, como ocurre en estructuras similares al problema XOR, una única frontera lineal no puede resolver correctamente todas las muestras. Esta limitación es relevante para entender por qué el experimento sintético incluye un conjunto no linealmente separable.

\begin{figure}[H]
\centering
\fbox{\parbox{0.75\textwidth}{\centering
\vspace{1.2cm}
\pendiente{Insertar diagrama conceptual del caso XOR o de clases no linealmente separables.}
\vspace{1.2cm}}}
\caption{Ejemplo conceptual de un problema no linealmente separable.}
\end{figure}

El entrenamiento del perceptrón actualiza los pesos usando únicamente las muestras mal clasificadas. Si $U$ es el conjunto de dichas muestras, una actualización típica puede escribirse como

\begin{equation}
\mathbf{w}_{k+1} = \mathbf{w}_{k} + \alpha \sum_{\mathbf{x}_i \in U} t_i \mathbf{x}_i,
\end{equation}

donde $\alpha$ es la tasa de aprendizaje. El teorema de convergencia del perceptrón establece que, si los datos son linealmente separables, existe un margen positivo y se usa una tasa de aprendizaje constante, el algoritmo encuentra una solución separadora en un número finito de actualizaciones. La intuición de la demostración es que cada actualización aumenta la alineación entre los pesos y algún vector separador óptimo, mientras que el crecimiento de la norma de los pesos queda acotado; ambas condiciones implican que no puede haber infinitos errores si existe una separación lineal.

En el perceptrón clásico, usar $\alpha = 1$ es suficiente porque la tasa de aprendizaje solo escala el tamaño de las actualizaciones. Para datos linealmente separables, cambiar $\alpha$ modifica la escala de los pesos, pero no necesariamente la orientación de la frontera final. En problemas no separables o en variantes con criterios de parada prácticos, la elección de $\alpha$ sí puede afectar la estabilidad del entrenamiento y el comportamiento observado.

\section{Regresión logística}

La regresión logística es un modelo estadístico de clasificación binaria desarrollado formalmente para analizar variables de respuesta dicotómicas. Una formulación influyente fue presentada por Cox en 1958 para secuencias binarias \cite{cox1958binary}. En lugar de producir directamente una clase discreta, el modelo estima la probabilidad de pertenencia a una clase mediante una función sigmoide aplicada sobre una combinación lineal de las entradas:

\begin{equation}
\hat{y} = \sigma(\mathbf{x}^{T}\mathbf{w}) =
\frac{1}{1 + e^{-\mathbf{x}^{T}\mathbf{w}}}.
\end{equation}

La salida $\hat{y}$ pertenece al intervalo $[0,1]$, por lo que puede interpretarse como una probabilidad. Para convertirla en una clase se usa un umbral, usualmente $0.5$. Esta diferencia respecto al perceptrón es importante porque la sigmoide es diferenciable y permite ajustar los pesos mediante gradiente. Además, la regresión logística es ampliamente usada en problemas de clasificación binaria por su interpretabilidad y eficiencia \cite{intro_regresion}.

Matemáticamente, la sigmoide suaviza la decisión. Valores muy negativos de $\mathbf{x}^{T}\mathbf{w}$ se aproximan a $0$, valores muy positivos se aproximan a $1$, y valores cercanos a cero quedan cerca de $0.5$. Por eso, el modelo no solo indica una clase, sino también qué tan cerca está una muestra de la frontera de decisión.

\subsection{Diferencias respecto al perceptrón}

La implementación de regresión logística se construye usando como base la estructura del perceptrón, pero modifica tres componentes esenciales: la función de activación, el cálculo del gradiente y la función de error. Estas diferencias no son solo detalles de implementación; cambian la naturaleza del entrenamiento.

\begin{table}[H]
\centering
\caption{Diferencias principales entre perceptrón y regresión logística.}
\begin{tabular}{p{0.22\textwidth}p{0.32\textwidth}p{0.32\textwidth}}
\toprule
Componente & Perceptrón & Regresión logística \\
\midrule
Salida & Clase discreta obtenida con la función signo. & Probabilidad en $[0,1]$ obtenida con sigmoide. \\
Activación & No diferenciable en la frontera. & Diferenciable, útil para optimización por gradiente. \\
Uso de muestras & Actualiza con muestras mal clasificadas. & Todas las muestras aportan al gradiente. \\
Error & Criterio basado en errores de clasificación. & Error continuo entre etiqueta y probabilidad estimada. \\
Interpretación & Separador lineal binario. & Separador lineal probabilístico. \\
\bottomrule
\end{tabular}
\end{table}

En el perceptrón, la función de activación puede representarse como:

\begin{algorithm}[H]
\caption{Activación del perceptrón}
\begin{algorithmic}[1]
\Function{ActivacionPerceptron}{$X, w$}
    \State \Return $\operatorname{sign}(Xw)$
\EndFunction
\end{algorithmic}
\end{algorithm}

En la regresión logística, esta activación se reemplaza por:

\begin{algorithm}[H]
\caption{Activación de regresión logística}
\begin{algorithmic}[1]
\Function{ActivacionLogistica}{$X, w$}
    \State \Return $\sigma(Xw)$
\EndFunction
\end{algorithmic}
\end{algorithm}

El gradiente implementado para la regresión logística sigue la forma vectorizada:

\begin{equation}
\nabla =
\frac{1}{N}\sum_{i=1}^{N} \mathbf{x}_i(t_i - \hat{y}_i).
\end{equation}

Esta forma aparece al medir la discrepancia entre la etiqueta real $t_i$ y la probabilidad estimada $\hat{y}_i$. Si el modelo predice una probabilidad menor que la etiqueta real, el término $(t_i-\hat{y}_i)$ empuja los pesos en dirección de $\mathbf{x}_i$; si predice una probabilidad demasiado alta, el término tiene signo contrario. Así, cada muestra aporta información proporcional al error cometido, no solo una señal binaria de correcto o incorrecto.

La actualización de pesos resultante es

\begin{equation}
\mathbf{w}_{k+1} = \mathbf{w}_{k} + \alpha \nabla.
\end{equation}

Esta formulación es más informativa que la actualización del perceptrón para problemas con incertidumbre o solapamiento entre clases. El perceptrón reacciona únicamente cuando una muestra queda mal clasificada; la regresión logística, en cambio, ajusta los pesos incluso cuando la clasificación discreta es correcta pero la probabilidad estimada todavía está lejos de la etiqueta. Por eso puede producir curvas de error más suaves y análisis más detallados durante el entrenamiento.

\section{Resultados con datos sintéticos}

Esta sección evalúa la regresión logística en dos escenarios controlados: datos linealmente separables y datos no linealmente separables. El objetivo no es solamente comparar errores, sino observar cómo la forma geométrica del conjunto de datos afecta la frontera de decisión aprendida.

\subsection{Datos linealmente separables}

Para generar los datos linealmente separables se utiliza la función \texttt{make\_blobs} de \texttt{scikit-learn}, configurada con dos centros y baja desviación de agrupamiento. Esta elección produce dos nubes de puntos compactas, por lo que una frontera lineal debería separar las clases con bajo error.

En este caso, se espera que el error disminuya de forma clara durante el entrenamiento y que la superficie de decisión quede ubicada entre los dos grupos de puntos. Para graficar dicha superficie, se parte de la ecuación lineal aprendida por el modelo:

\begin{equation}
w_0 + w_1x_1 + w_2x_2 = 0.
\end{equation}

Despejando $x_2$, se obtiene:

\begin{equation}
x_2 = -\frac{w_0 + w_1x_1}{w_2}.
\end{equation}

Esta recta se superpone al diagrama de dispersión para visualizar la separación inducida por los pesos aprendidos.

\begin{figure}[H]
\centering
\begin{subfigure}{0.48\textwidth}
    \centering
    \fbox{\parbox{0.9\linewidth}{\centering \vspace{1cm}\pendiente{Dispersión + frontera}\vspace{1cm}}}
    \caption{Superficie de decisión.}
\end{subfigure}
\hfill
\begin{subfigure}{0.48\textwidth}
    \centering
    \fbox{\parbox{0.9\linewidth}{\centering \vspace{1cm}\pendiente{Curva de MAE}\vspace{1cm}}}
    \caption{Error por iteración.}
\end{subfigure}
\caption{Resultados visuales para datos linealmente separables.}
\end{figure}

\subsection{Datos no linealmente separables}

Para generar los datos no linealmente separables se utiliza la función \texttt{make\_moons} de \texttt{scikit-learn}, con ruido agregado. Esta función produce dos clases con forma de medias lunas entrelazadas, una estructura que no puede separarse completamente con una única recta.

En este caso, una frontera lineal resulta insuficiente para separar por completo las clases. Por tanto, aunque el entrenamiento puede reducir el error, se espera que el MAE final sea mayor que en el caso linealmente separable. Este resultado no indica necesariamente una falla de implementación; más bien evidencia una limitación esperada del modelo lineal.

\begin{figure}[H]
\centering
\begin{subfigure}{0.48\textwidth}
    \centering
    \fbox{\parbox{0.9\linewidth}{\centering \vspace{1cm}\pendiente{Dispersión + frontera}\vspace{1cm}}}
    \caption{Superficie de decisión.}
\end{subfigure}
\hfill
\begin{subfigure}{0.48\textwidth}
    \centering
    \fbox{\parbox{0.9\linewidth}{\centering \vspace{1cm}\pendiente{Curva de MAE}\vspace{1cm}}}
    \caption{Error por iteración.}
\end{subfigure}
\caption{Resultados visuales para datos no linealmente separables.}
\end{figure}

\subsection{Resultados promedio para 10 corridas}

En esta sección se reporta el promedio del MAE y su desviación estándar en 10 corridas, generando datos aleatorios en cada una. La implementación registra para cada corrida el promedio del MAE durante entrenamiento, la desviación estándar del MAE durante entrenamiento y el MAE obtenido con datos de prueba. Para facilitar la lectura, la tabla principal resume los agregados, mientras que las tablas por corrida pueden incluirse como anexos o como tablas complementarias.

\begin{table}[H]
\centering
\caption{Resumen de resultados de regresión logística con datos sintéticos.}
\label{tab:synthetic-results}
\begin{tabular}{lll}
\toprule
Métrica & Linealmente separable & No linealmente separable \\
\midrule
Promedio de MAE en prueba & \pendiente{Completar} & \pendiente{Completar} \\
Desviación estándar del MAE de entrenamiento & \pendiente{Completar} & \pendiente{Completar} \\
Mejor MAE observado en prueba & \pendiente{Completar} & \pendiente{Completar} \\
\bottomrule
\end{tabular}
\end{table}

\section{Regresión logística con TF-IDF}

TF-IDF (\textit{Term Frequency-Inverse Document Frequency}) es una representación clásica para documentos. Su objetivo es asignar mayor peso a términos frecuentes en un documento, pero menos comunes en el corpus completo. De esta manera, palabras muy generales pierden relevancia, mientras que términos más distintivos adquieren mayor peso.

En este proyecto, TF-IDF se usa como etapa de extracción de características. La implementación utiliza \texttt{TfidfVectorizer} de \texttt{scikit-learn} con un analizador personalizado. Este analizador aplica el preprocesamiento definido en el proyecto antes de construir el vocabulario y calcular los pesos TF-IDF. Luego, la matriz dispersa producida por el vectorizador se transforma con \texttt{toarray()} y se convierte a un tensor \texttt{torch.float32}, que es el formato esperado por la regresión logística implementada previamente.

\subsection{Preprocesamiento}

Antes de fijar una configuración para TF-IDF se realizó una comparación exploratoria entre varias estrategias de preprocesamiento. El objetivo no era solamente obtener una métrica, sino entender qué decisiones lingüísticas afectaban la representación del texto financiero. En particular, se compararon tokenizadores basados en NLTK, expresiones regulares, spaCy y Stanza; además, se probaron lematizadores basados en WordNet, Simplemma, spaCy y Stanza.

Una decisión importante fue probar expresiones regulares con y sin números. Para ello se usaron dos patrones principales: \texttt{[\textasciicircum\textbackslash W\_]+}, que conserva secuencias alfanuméricas, y \texttt{[a-záéíóúñü]+}, que conserva solamente letras. Esta diferencia es relevante porque los textos financieros contienen porcentajes, montos, fechas y referencias numéricas; sin embargo, también existe el riesgo de que los números introduzcan ruido o vocabulario demasiado específico para un conjunto de datos pequeño. Por esta razón, no se asumió de antemano que conservar números fuera mejor: se evaluaron ambas opciones.

La comparación incluyó configuraciones permisivas, que conservan símbolos como \texttt{\$} o \texttt{\%}, y configuraciones más restrictivas, que eliminan ese tipo de marcas. También se evaluaron variantes con y sin números. Esta exploración permitió observar que el mejor preprocesamiento no depende únicamente de usar una herramienta más sofisticada: en un corpus pequeño, una representación más simple puede generalizar igual o mejor que una canalización pesada si reduce ruido y mantiene los términos relevantes.

\begin{table}[H]
\centering
\caption{Alternativas evaluadas para el preprocesamiento de TF-IDF.}
\begin{tabular}{p{0.30\textwidth}p{0.22\textwidth}p{0.22\textwidth}p{0.18\textwidth}}
\toprule
Configuración & Tokenización & Lematización & Propósito \\
\midrule
NLTK modo estándar & \texttt{word\_tokenize} & WordNet & Comparar contra tokenización lingüística básica. \\
Regex con números & \texttt{[\textasciicircum\textbackslash W\_]+} & WordNet o Simplemma & Medir si cantidades y referencias numéricas aportan información. \\
Regex sin números & \texttt{[a-záéíóúñü]+} & WordNet o Simplemma & Reducir ruido numérico y conservar vocabulario léxico. \\
spaCy modo estándar & Tokenización de spaCy & spaCy & Evaluar una canalización lingüística robusta. \\
spaCy modo permisivo & Tokenización de spaCy & spaCy & Conservar símbolos potencialmente financieros. \\
Stanza modo estándar & Tokenización de Stanza & Stanza & Comparar con lematización neuronal en español. \\
Stanza modo permisivo & Tokenización de Stanza & Stanza & Evaluar si conservar más marcas mejora la señal. \\
\bottomrule
\end{tabular}
\end{table}

Para evitar que la comparación dependiera de una única partición del conjunto de datos, cada alternativa se evaluó con varias semillas. En cada semilla se entrenó la misma regresión logística y se compararon dos escenarios: una tasa de aprendizaje fija y una tasa de aprendizaje calibrada con Optuna. La calibración no cambia el descriptor TF-IDF; solamente busca que cada representación sea evaluada con un valor razonable de \texttt{alpha}. De esta forma, la comparación distingue mejor entre la calidad de la representación textual y una mala elección accidental de la tasa de aprendizaje.

En las pruebas reducidas realizadas, la variante \texttt{nltk modo regex sin numeros} obtuvo el mejor MAE promedio calibrado. Esto sugiere que, para este subconjunto del corpus FEINA, eliminar números produjo una representación más estable que conservarlos. La interpretación debe ser prudente: no significa que los números sean irrelevantes en finanzas, sino que en este experimento concreto la regresión logística con TF-IDF aprovechó mejor el vocabulario léxico que las marcas numéricas.

\subsection{Construcción de descriptores}

Una vez preprocesados los documentos, \texttt{TfidfVectorizer} aprende el vocabulario del corpus y construye una matriz documento-término. Cada fila representa un texto y cada columna representa un término del vocabulario aprendido. El valor de cada celda indica la importancia relativa del término en ese documento.

Para examinar cualitativamente esta representación, se seleccionarán textos simples y complejos y se reportarán los términos con mayor peso TF-IDF. Esto permitirá verificar si el descriptor captura vocabulario relevante para complejidad financiera o si queda dominado por términos poco informativos. En el caso de las variantes con números, una parte del vocabulario puede corresponder a cantidades, porcentajes o referencias aisladas que no necesariamente distinguen complejidad textual. Por eso se consideró razonable elegir una configuración sin números para la comparación principal, manteniendo el análisis de variantes como evidencia de diseño experimental.

\subsection{Clasificación y resultados}

El conjunto FEINA se procesa usando sus columnas \texttt{Segment} y \texttt{Proposal}. En la implementación actual, cada \texttt{Segment} se agrega al corpus con etiqueta \claseCompleja{}, y cada \texttt{Proposal} se agrega con etiqueta \claseSimple{}. Así, cada par del archivo aporta dos muestras al clasificador: una compleja y una simple.

Después de construir la matriz TF-IDF, se entrena la regresión logística usando esas etiquetas binarias. La comparación se realizó manteniendo constante el clasificador y variando únicamente el preprocesamiento. Esta decisión es importante: si se cambiara al mismo tiempo el clasificador, la tokenización y la calibración del entrenamiento, sería más difícil atribuir las diferencias de rendimiento a una causa concreta.

\begin{table}[H]
\centering
\caption{Resultados de regresión logística con TF-IDF.}
\begin{tabular}{ll}
\toprule
Métrica & Valor \\
\midrule
MAE promedio & \pendiente{Completar} \\
Exactitud promedio & \pendiente{Completar} \\
Desviación estándar del MAE & \pendiente{Completar} \\
Desviación estándar de exactitud & \pendiente{Completar} \\
Tamaño promedio del vocabulario & \pendiente{Completar} \\
Configuración de preprocesamiento & Regex sin números + NLTK stopwords + lematización \\
\bottomrule
\end{tabular}
\end{table}

\section{Regresión logística con vectores empotrados}

Los vectores empotrados representan textos en un espacio numérico denso, donde la cercanía geométrica intenta reflejar similitud semántica. A diferencia de TF-IDF, que produce vectores dispersos basados en conteos ponderados, los embeddings capturan información distribuida aprendida previamente a partir de grandes corpus.

En este trabajo se compararon varias formas de proyectar los textos de FEINA a un espacio vectorial. Posteriormente, esos vectores se clasificaron con la misma regresión logística implementada en la primera parte. Esta separación mantiene constante el clasificador y permite analizar si las diferencias provienen de la representación del texto.

La comparación incluyó cuatro modelos: \texttt{sentence-transformers/distiluse-base-multilingual-cased-v1}, \texttt{bert-base-multilingual-cased}, \texttt{dccuchile/bert-base-spanish-wwm-cased} (BETO) y una variante de RoBERTa. La intención fue probar distintas formas razonables de obtener vectores densos para los textos y observar cuál funcionaba mejor al entrenar la misma regresión logística.

\subsection{Proyección de textos}

La proyección transforma cada documento en un vector de dimensión fija. La idea central es que textos con significado parecido queden representados por vectores cercanos, incluso si no comparten exactamente las mismas palabras. Esto es útil para el proyecto porque la complejidad textual no depende solo de términos aislados: también influyen la forma de expresar una idea, la densidad conceptual y el contexto semántico de la oración.

Esta representación se evaluó como una alternativa a TF-IDF. Mientras TF-IDF se basa en frecuencia de términos y puede funcionar muy bien cuando ciertas palabras distinguen clases, los embeddings permiten aprovechar conocimiento aprendido previamente por modelos entrenados en grandes corpus. Por ejemplo, dos textos financieros pueden usar vocabulario distinto pero expresar una idea parecida; una representación densa puede capturar parte de esa relación mejor que una bolsa de palabras. Por eso ambos enfoques se comparan: no porque uno sea una extensión directa del otro, sino porque representan dos maneras distintas de alimentar el mismo clasificador.

Por esta razón se probaron varias alternativas de embeddings. El modelo \texttt{distiluse-base-multilingual-cased-v1} fue relevante porque está diseñado para producir embeddings de oración directamente. BERT, BETO y RoBERTa se usaron como alternativas contextuales basadas en transformadores, con el fin de revisar si otra representación densa podía producir mejores resultados con la misma regresión logística.

\begin{table}[H]
\centering
\caption{Modelos evaluados para generar vectores empotrados.}
\begin{tabular}{p{0.35\textwidth}p{0.22\textwidth}p{0.32\textwidth}}
\toprule
Modelo & Dimensión & Razón para evaluarlo \\
\midrule
\texttt{distiluse-base-multilingual-cased-v1} & 512 & Modelo preparado para embeddings de oración. \\
\texttt{bert-base-multilingual-cased} & 768 & Alternativa contextual basada en BERT. \\
\texttt{dccuchile/bert-base-spanish-wwm-cased} & 768 & Alternativa contextual basada en BETO. \\
RoBERTa español & 768 & Alternativa contextual basada en RoBERTa. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Clasificación y resultados}

Una vez obtenidos los embeddings, se entrena la regresión logística sobre esos vectores. En todos los casos se conserva el mismo protocolo: se usan las mismas etiquetas, particiones estratificadas, varias semillas y la misma calibración de \texttt{alpha}. Por tanto, el experimento puede interpretarse como una comparación entre representaciones densas del texto, no como una comparación entre clasificadores distintos.

Las pruebas exploratorias mostraron que BETO puede ofrecer una señal competitiva en esta configuración reducida. Sin embargo, el modelo de \texttt{SentenceTransformer} sigue siendo relevante porque produce directamente embeddings de oración y es simple de integrar. Esto hace que sea una alternativa razonable cuando se busca una representación semántica robusta sin entrenar un modelo profundo desde cero.

La conclusión metodológica es que la elección de embeddings no se hizo por preferencia arbitraria, sino mediante pruebas controladas. Se entrenó la misma regresión logística sobre cada representación para observar qué tipo de vector describe mejor los textos de FEINA para la tarea de complejidad.

\begin{table}[H]
\centering
\caption{Resultados de regresión logística con vectores empotrados.}
\begin{tabular}{ll}
\toprule
Métrica & Valor \\
\midrule
MAE promedio & \pendiente{Completar} \\
Exactitud promedio & \pendiente{Completar} \\
Desviación estándar del MAE & \pendiente{Completar} \\
Desviación estándar de exactitud & \pendiente{Completar} \\
Modelo de embeddings & \pendiente{Completar con el modelo seleccionado en la prueba final} \\
Dimensión del vector & \pendiente{Completar según el modelo seleccionado} \\
\bottomrule
\end{tabular}
\end{table}

\section{Modelos de lenguaje grandes (LLMs)}

Los modelos de lenguaje grandes o \textit{Large Language Models} (LLMs) permiten abordar la clasificación de textos mediante instrucciones en lenguaje natural. En lugar de entrenar explícitamente un clasificador sobre características numéricas, se formula un prompt que describe la tarea y solicita una etiqueta. Esta aproximación reduce la ingeniería de características, pero introduce otros retos: sensibilidad al prompt, costo computacional, consumo de memoria, tiempo de inferencia y dificultad para controlar la salida exacta.

En este trabajo se evalúan dos LLMs de Hugging Face. Ambos reciben instrucciones para clasificar cada texto como simple o complejo usando las clases \claseSimple{} y \claseCompleja{}, respectivamente. Para que la comparación sea justa, los modelos deben usar el mismo conjunto de textos, el mismo formato de salida esperado y, en el caso few-shot, los mismos ejemplos demostrativos.

\subsection{SmolLM3}

SmolLM3 es un modelo instruccional de Hugging Face usado como una alternativa relativamente compacta para clasificación mediante prompts. En este proyecto se utiliza con un prompt que define el rol del modelo como clasificador de textos financieros, especifica las clases válidas y exige una respuesta en formato \texttt{ID:CLASE}. La elección de este formato permite evaluar automáticamente lotes de textos sin interpretar respuestas largas o ambiguas.

\pendiente{Agregar configuración final: precisión numérica, dispositivo, máximo de tokens, tamaño de lote y tiempo aproximado de inferencia.}

\subsection{Phi-4 mini}

Phi-4 mini instruct se evalúa como segundo LLM para comparar el comportamiento de otro modelo instruccional ante la misma tarea. Al igual que con SmolLM3, se mantiene el mismo esquema de clases, el mismo formato de salida y el mismo criterio de evaluación. Esta consistencia es importante porque, si los prompts o ejemplos cambian entre modelos, la comparación deja de medir únicamente diferencias entre modelos.

\pendiente{Agregar configuración final: precisión numérica, dispositivo, máximo de tokens, tamaño de lote y tiempo aproximado de inferencia.}

\subsection{Clasificación sin few-shot}

En la configuración sin few-shot, el prompt describe la tarea y el formato de salida, pero no proporciona ejemplos etiquetados. Esta condición evalúa la capacidad del modelo para interpretar la instrucción desde su conocimiento previo. Durante las pruebas se compararon variantes de prompt y se eligió la que produjo mejor adherencia al formato solicitado y mejores resultados preliminares.

El formato de respuesta esperado debe ser estricto para facilitar la evaluación automática:

\begin{quote}
\texttt{ID:CLASE}
\end{quote}

Por ejemplo, \texttt{1:0} indica que el texto con identificador 1 fue clasificado como simple. En la implementación, cualquier línea que no cumpla el patrón exacto \texttt{ID:CLASE}, donde \texttt{CLASE} debe ser \texttt{0} o \texttt{1}, se registra como \texttt{-1}. Esta decisión evita detener la ejecución por respuestas mal formadas y, al mismo tiempo, impide que una salida inválida sea contada como correcta. Así, el resultado refleja tanto la capacidad de clasificar como la capacidad de seguir instrucciones.

\begin{table}[H]
\centering
\caption{Ejemplos de clasificación sin few-shot.}
\begin{tabular}{p{0.18\textwidth}p{0.44\textwidth}p{0.15\textwidth}p{0.15\textwidth}}
\toprule
Modelo & Texto & Etiqueta real & Predicción \\
\midrule
\pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} \\
\pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} \\
\pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} \\
\bottomrule
\end{tabular}
\end{table}

\pendiente{Los ejemplos se deben registrar manualmente a partir de las salidas de los notebooks, porque actualmente el código imprime ejemplos pero no los exporta como tabla.}

\subsection{Clasificación con few-shot}

El few-shot learning agrega ejemplos etiquetados dentro del prompt antes de solicitar la clasificación de nuevos textos. La hipótesis es que estos ejemplos ayudan al modelo a calibrar la definición operacional de simple y complejo en el dominio financiero. Sin embargo, la literatura reciente también advierte que los LLMs pueden tener dificultades en escenarios zero-shot y few-shot cuando la tarea requiere razonamiento complejo o conocimiento específico del dominio \cite{chamieh2024llms}.

Para que el experimento sea comparable, los mismos ejemplos deben usarse en ambos modelos y en todas las configuraciones equivalentes. Además, los ejemplos usados como shots no deben volver a evaluarse como parte del conjunto de prueba de esa configuración.

\begin{table}[H]
\centering
\caption{Configuraciones few-shot.}
\label{tab:few-shot-configs}
\begin{tabular}{llll}
\toprule
Configuración & Cantidad de ejemplos & Criterio de selección & Modelos evaluados \\
\midrule
FS-1 & \pendiente{Completar} & Mismos ejemplos para ambos modelos & SmolLM3, Phi-4 mini \\
FS-2 & \pendiente{Completar} & Mismos ejemplos para ambos modelos & SmolLM3, Phi-4 mini \\
FS-3 & \pendiente{Completar} & Mismos ejemplos para ambos modelos & SmolLM3, Phi-4 mini \\
\bottomrule
\end{tabular}
\end{table}

\section{Prueba completa}

La prueba completa integra todos los tratamientos definidos anteriormente. Se realizarán 30 corridas con particiones aleatorias 80\% entrenamiento y 20\% prueba. En cada corrida, la partición debe mantenerse constante para todos los tratamientos, de modo que las diferencias observadas se atribuyan al modelo y no a variaciones en los datos evaluados.

Para registrar los resultados de forma suficiente para la prueba estadística, cada corrida debe conservar al menos el identificador de la semilla, el tratamiento evaluado, el MAE y la exactitud obtenida. Si se desea reproducir exactamente las particiones con LLMs, también conviene conservar los índices usados como prueba en cada semilla.

Los tratamientos a comparar son:

\begin{enumerate}
    \item Regresión logística con TF-IDF.
    \item Regresión logística con vectores empotrados.
    \item SmolLM3 sin few-shot y con few-shot.
    \item Phi-4 mini sin few-shot y con few-shot.
\end{enumerate}

\begin{table}[H]
\centering
\caption{Resultados para la prueba completa de 30 corridas.}
\begin{tabular}{lllll}
\toprule
Tratamiento & MAE promedio & Exactitud promedio & Desv. estándar MAE & Desv. estándar exactitud \\
\midrule
TF-IDF + regresión logística & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} \\
Embeddings + regresión logística & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} \\
SmolLM3 & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} \\
Phi-4 mini & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Estructura recomendada para registrar resultados por corrida.}
\begin{tabular}{lllll}
\toprule
Semilla & Tratamiento & MAE & Exactitud & Observaciones \\
\midrule
\pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} \\
\pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} \\
\pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} & \pendiente{Completar} \\
\bottomrule
\end{tabular}
\end{table}

\section{Prueba estadística}

Después de obtener los resultados de las 30 corridas, se realiza una prueba estadística para determinar si las diferencias entre tratamientos son atribuibles a variación aleatoria o si existe evidencia de diferencias sistemáticas de rendimiento.

Dado que se comparan más de dos tratamientos sobre las mismas particiones, una opción natural es un ANOVA de medidas repetidas si se cumplen sus supuestos. En este diseño, cada corrida funciona como bloque o sujeto experimental, y cada tratamiento se evalúa sobre la misma partición. La hipótesis nula es que todos los tratamientos tienen el mismo rendimiento promedio:

\begin{equation}
H_0: \mu_1 = \mu_2 = \cdots = \mu_k.
\end{equation}

La hipótesis alternativa indica que al menos un tratamiento tiene una media distinta:

\begin{equation}
H_1: \exists i,j \; \mu_i \neq \mu_j.
\end{equation}

Antes de aplicar ANOVA se deben revisar sus supuestos, especialmente normalidad de residuos y esfericidad. Si estos supuestos no se cumplen, se debe considerar una alternativa no paramétrica, como la prueba de Friedman. En caso de encontrar diferencias significativas, se recomienda aplicar una prueba post-hoc para identificar entre cuáles tratamientos aparecen dichas diferencias.

\pendiente{Una vez ejecutadas las 30 corridas, agregar prueba de supuestos, estadístico, valor $p$, tamaño de efecto y análisis post-hoc si corresponde.}

\section{Conclusiones}

Este trabajo compara representaciones clásicas, representaciones semánticas y modelos generativos instruidos para la clasificación de complejidad textual en español financiero. La estructura experimental permite observar no solo qué modelo obtiene mejores métricas, sino también qué tipo de representación resulta más adecuada para un dominio especializado y sensible a decisiones lingüísticas como tokenización, eliminación de palabras vacías y lematización.

\pendiente{Redactar las conclusiones finales cuando estén completos los resultados de TF-IDF, embeddings, LLMs, few-shot, prueba completa y prueba estadística.}

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
